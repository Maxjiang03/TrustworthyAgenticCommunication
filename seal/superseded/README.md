The manifest and OpenTimestamps proof in this directory describe commit 20f68f5 and are SUPERSEDED: after they were built and anchored, commit aeee0ea changed docs/PRE_REGISTRATION.md, which the manifest covers, so the seal was rebuilt over the later candidate. Kept, not deleted, because the .ots proof anchors these exact manifest bytes and a superseded record that vanishes is indistinguishable from one that never existed. The sealing manifest is ../manifest_v0.5.json.
