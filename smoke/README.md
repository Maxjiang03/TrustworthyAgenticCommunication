# Smoke Gates — Status Board

Feasibility smoke gates per `docs/EXPERIMENT_ARCHITECTURE_FINAL.md` Part G. **No v0.5, no
sealing, no confirmatory corpus until every in-scope gate passes on the pilot corpus.** Each gate
is a minimal runnable spike, not an experiment. A failing gate stops its branch and applies the
gate-outcome policy (fallbacks), recorded as an ADR.

**Execution DAG (respect dependencies):**

```
G-1 / G-5 / G-8  →  G-6 / G-7  →  G-2 / G-4 / G-11  →  G-12 / G-13  →  G-9 / G-14  →  G-10
```

G-3 (latency spike) may run any time after G-1, but its threshold is fixed beforehand
(`docs/frozen_parameters.md` item 2, currently UNSET). Claim-dependent gates (G-3, G-14, G-15,
INV-only check) run only for claims retained in the sealed scope.

Run a gate: `make gate GATE=g1`

**Apparatus note, 2026-07-30 (no gate status changed by it).** The experimental apparatus the
waiting gates need now exists: the golden thread runs end to end — Supervisor → A2A delegation
hop (behind a port; `a2a-python` still unpinned, ADR 0020) → Specialist → MCP tool call over the
frozen `Ω` — under **`B0`** (no delegation protection) and **`B3`** (the full control layer, its
ten §A.5 conjuncts each a named function with its own reason code), with the pilot corpus
(`fixtures/pilot/golden_thread/`), the harness runner, the SUT-side capability signer (D21's
independent implementation, agreement-tested against the harness verifier), and Phase-1 OAuth
provisioning (ADR 0021). **This pass adjudicated no gate, edited no row below, and set no
evidence grade**; every "not started" is still not started, and `IA-3` remains
`[UNVERIFIED-IA]` — the RQ4 timing seams exist but **nothing was measured** (rows 1–2 UNSET).

**Apparatus note, 2026-07-31.** *(The note above was true when written; this supersedes its arm
list and nothing else.)* Two more arms exist — **`B2-exchange-task`** (the fair strong OAuth arm:
online RFC 8693 exchange per hop, narrowed to `C_i` under the ADR 0017 RAR type) and **`B-cap`**
(offline attenuation, bearer capability, `oauth_authn = 1`) — over a **four-scenario** pilot
corpus, the fourth being `gt-f1-chain-tamper`. `frozen_parameters` rows 4/6/10 are frozen as
`H(Λ)` (ADR 0022, composition amended by ADR 0023) and the delegating client's Phase-1 grant is
fixed by ADR 0024. **G-13 is adjudicated by this pass and its row below is updated; no other row
moved**, and `IA-3` is untouched — G-13 establishes matched authority, not cost, and **no timing
number was produced anywhere**.

**Apparatus note, 2026-07-31 (second pass).** The SS E.1 ladder is now **complete**: `B1`, `B2-broad-noexchange`, `B2-exchange-broad`, `B2-exchange-task-DPoP` and `B3⁺` join the four that existed, so all **nine** arms are built and the full nine-arm × four-scenario matrix agrees with §E.4 in every cell. `frozen_parameters` rows **1, 2, 3 and 7** are set (ADR 0025/0026/0027) and row **5 is deferred by decision** (ADR 0028), leaving only row 9, which is read off the measurement box at seal time. **G-13 was re-adjudicated over all five strong baselines and its two open limbs are CLOSED**; its row below records the previous wording as a dated update rather than deleting it. **No other gate's row moved.** `IA-3` and `IA-9` are both untouched and both stay `[UNVERIFIED-IA]` — rows 1/2 being set does not authorize measurement, and building `B3⁺`'s `jti` cache is not running G-9. **No timing number was produced anywhere.**

| Gate | Depends on | Status | Report | ADR |
|------|-----------|--------|--------|-----|
| G-1 | — | **PASS** — restored after the ADR 0003 corrective suite passed (10/10 regression tests): commitment is now the versioned BlockID scheme (encoding-independent, fail-closed); biscuit-python==0.4.0 pinned; G-1.G′ replaced the seal check (ADR 0002) | [g1/REPORT.md](g1/REPORT.md) | [0002](../adr/0002-python-biscuit-library.md), [0003](../adr/0003-capability-commitment-scheme.md) |
| G-2 | G-6/G-7 | **PASS** — the first authorizer-with-policies run in the project, against the `Ω`/`Γ` frozen by ADR 0016 (`H(Γ) = f63320c9da3731a6ea04dc51d9f6852f3a3e130182ce3a7fe251158751333deb`), with every `C_i` **computed** by 7 authorizer runs per prefix, never asserted: (a) a widening append verifies under `κ_pub` yet leaves `C_i ⊆ C_{i−1}` — extended to six broadening vectors incl. rules, `expiry` and `token_audience`, two probed under their unlocking condition — while legitimate narrowing gives `C_2 ⊊ C_1 ⊊ C_0 = U_task`; (b) a **fully constructed** attacker-signed third-party block is rejected structurally pre-evaluation (the library itself verifies it under `κ_pub`, so the project check is load-bearing) and carries no authority if evaluated anyway, and a `trusting {attacker_key}` authorizer is refused pre-evaluation though it would otherwise admit; (c) five trust-broadening/`Ω`-widening mutations each change `H(Γ)` while a re-serialization does not, artifact byte-intact; (d) `−attenuation` admits exactly `C_0 \ C_n` more, differing in exactly `evaluation.prefix`. **IA-2 verified by this gate**; residuals: exact pin, scoped to these frozen bytes, SUT-side independent implementation still owed (D13/D21, G-13) | [g2/REPORT.md](g2/REPORT.md) | [0002](../adr/0002-python-biscuit-library.md), [0003](../adr/0003-capability-commitment-scheme.md), [0016](../adr/0016-omega-gamma-freeze.md) |
| G-3 | G-1 (threshold fixed beforehand) | **PASS** — the **first and only** timing measurement in this repository, taken **once** on the row 9 sealed platform, which was **locked in a separate commit beforehand** (`docs/measurement_platform.md`). The quantity is `boundary_verification` **alone** — `arm.decide(...)` — and **not** row 1's `presentation + boundary_verification` segment; ADR 0026 separated them and the two bars differ on purpose. 1,000 HTC+INV pairs signed and verified per Part G's row, 4 batches x 250, 100 warm-up iterations discarded (fixed in advance, not fitted to the curve). **Median is below the ADR 0025 threshold of 5 ms**, which was fixed before any measurement existed precisely so the gate could fail; the figures live **only** in [g3/REPORT.md](g3/REPORT.md), because the repository-wide no-timing-number invariant became narrower rather than void. **Three hazards handled, not hoped away:** the P/E-core mask was **DETECTED** through `GetSystemCpuSetInformation` and the process pinned to the highest efficiency class (fail-closed — no mask is guessed, since a guessed range reintroduces the very bias pinning removes); **AC power asserted**, with a named refusal on battery; and four batches run so thermal drift would be **visible** — **none was observed**, the last batch being slightly faster than the first. **One finding recorded rather than smoothed:** the measured headroom is **thinner than ADR 0025's prose expected** (it argued three- to tenfold); the threshold was **not** moved and neither was the ADR's argument, and the discrepancy is stated in the report so a reader does not have to guess which was adjusted. **A Linux CI run is regression protection only and is NEVER adjudicative** — the spike prints NOT ADJUDICATED and records no verdict off-platform, the same rule G-12 applies to its ledger-backed limbs. **IA-3 moves to VERIFIED.** **Re-triggered by any change to row 9** — power plan, hardware, driver or Windows build | [g3/REPORT.md](g3/REPORT.md) | [0025](../adr/0025-llm-turn-denominators-and-the-g3-latency-threshold.md), [0026](../adr/0026-the-equivalence-margin-and-the-measured-segment.md), [0014](../adr/0014-ledger-platform-decision.md) |
| G-4 | G-6/G-7 | **PASS — now a full four-limb closure, completed by G-11.** It first passed over its *adjudicable* limbs only. Phase 1 read RFCs 8693/9396/8707/9449/9068 + OAuth 2.1 from the text and found `authlib==1.7.2` **UNSUPPORTED** ([g4/probe_authlib.py](g4/probe_authlib.py)), so **IA-4 is discharged by its second limb**: a behaviourally faithful AS **was built** at `src/sut/oauth_as/` (ADR 0015/0017) on the stdlib + `joserfc`, with **no new dependency pinned** and `authlib` still unpinned. Phase 2 adjudicated L1 (a task-narrowed token issues, granted RAR echoed, scope reported when it differs), **L1′** (widening refused with the exact error and **no token issued** in all four planes — asserted on the *absence* of a token, since a silent clamp is the failure mode), **L2** over the **frozen `Ω`** with no stand-in (both planes enforced; inside-RAR-outside-scope and inside-scope-outside-RAR both denied; wrong-`aud` token rejected), L3 (`actor→holder` resolves, unmapped actor rejected, `resource_owner = holder` never required, nested `act` present but only the outermost consulted), and A1–A7 (delegation not impersonation; 24 catalogue checks with exact codes/statuses; the RFC 9396 §12 string rule incl. NFC-vs-NFD; key isolation with both ADR 0015 import rules asserted and a seedless forge attempt rejected; and the **first real exercise** of the G-5 `ath` and nonce hand-forwards plus `htu` normalization). **Both residual limbs are now CLOSED by G-11** (2026-07-29, after this row first read "not a full four-limb closure"): **L4** — `INV.access_token_hash` verified through the real HTC/INV verifier with a swapped token rejected, the construction fixed by ADR 0018; and **L3** — the `actor→holder` limb re-run against the **frozen** registry (ADR 0019) instead of the C3 stand-in, outcome unchanged, which confirms the stand-in had not flattered it. **Still open:** `may_act` is spike-local pending `frozen_parameters` row 5, so F2 `wrong_principal` is not yet scored; and the AT profile is RFC 9068-**shaped**, deliberately **not** conformant (§8.3) | [g4/REPORT.md](g4/REPORT.md) · [g4/DESIGN.md](g4/DESIGN.md) (design) · [g4/SCOPE.md](g4/SCOPE.md) | [0008](../adr/0008-g4-spike-parallelisation.md), [0015](../adr/0015-as-placement.md), [0016](../adr/0016-omega-gamma-freeze.md), [0017](../adr/0017-as-profile-phase2.md), [0018](../adr/0018-access-token-hash.md), [0019](../adr/0019-identity-plane-registry.md) |
| G-5 | — | **PASS** — `joserfc==1.7.4` pinned for the JOSE surface only (ADR 0006): cnf/jkt binding with the RFC 8037 A.3 known answer, RFC 9449 §4.3 subset verifier, wrong-holder rejected at the thumbprint comparison; AS simulated locally — the real AS stays gated on G-4 (authlib pending line unchanged) | [g5/REPORT.md](g5/REPORT.md) | [0006](../adr/0006-dpop-jose-library.md) |
| G-6 | G-1/G-5/G-8 | **PASS** — `mcp==1.28.1` pinned (ADR 0013): full dispatch-path enumeration with file:symbol citations; wrap-at-fn + wrap-on-insert interposition mediates every enumerated path (7 bypass attempts all mediated or blocked); denied calls never execute (witness-verified); exactly one event on the error path; non-vacuity confirmed with the interposition removed | [g6/REPORT.md](g6/REPORT.md) | [0013](../adr/0013-mcp-sdk-pin.md) |
| G-7 | G-1/G-5/G-8 | **PASS** — exclusive-share ledger process (Win32 `FILE_SHARE_READ`-only handle): all SUT write/amend/delete attempts fail at the OS level (chmod-proof), records survive a lying SUT self-report, correlation ids intact, zero entries for unreached tools; `ingress_request_digest` settled as `H_JCS` (ADR 0012) | [g7/REPORT.md](g7/REPORT.md) | [0012](../adr/0012-ingress-digest-construction.md), [0013](../adr/0013-mcp-sdk-pin.md) |
| G-8 | — | **PASS** — `rfc8785==0.1.4` pinned (ADR 0005): encoding-invariant, RFC-vector-conformant (Appendix B 24/24; `canonicaljson` demonstrated non-conformant and rejected), separate-process signer/verifier agreement, fail-closed; the frozen `H_JCS` construction (hash/tag/encoding) recorded as an open decision | [g8/REPORT.md](g8/REPORT.md) | [0005](../adr/0005-jcs-library.md) |
| G-9 | G-12/G-13 | **PASS** — the replay cache is now a **single-writer arbiter process** (**ADR 0033**), so the §F.5 check-and-insert is atomic ACROSS processes: one process, one thread, one request in flight, which makes atomicity a property of the SHAPE rather than of a lock that could be mis-scoped. **Exactly one** of N=8 concurrent bit-identical requests proceeds — asserted as a COUNT, and over 20 trials the admitted count is `{1}` every time; *at least one blocked* would not have caught a cache admitting two. **The lock-removed world genuinely double-admits** (per-caller caches admit 8 of 8), so the result is a measurement and not a race that happened not to occur. **The induced backend error denies in both shapes** — an arbiter answering `error` and an arbiter killed mid-flight — with a healthy-arbiter positive control, so the denials are the failure and not a client that always denies. **Overflow is REACHED** at the real frozen 2^16, filled through the same `consume` path; the next distinct id fails closed and an entry inserted before the fill survives, so no unexpired entry was evicted (the flooding bypass ADR 0027 rejects). **The frozen budget is unmovable by absence**: the arbiter accepts no capacity or TTL flag. The clock stays injected and nothing sleeps. **IA-9 moves to VERIFIED.** **Scope:** multi-process replay detection only; `B3⁺`'s §E.5 bitmask is unchanged (where the cache runs is not a ladder property); **IA-3** stays `[UNVERIFIED-IA]` for G-3. *Update, 2026-08-02 — the L4 limb was FLAKY and the outcome is unchanged. EXP5 STEP 13's standing check re-ran every gate and G-9 **errored** with its other five limbs green: filling the frozen `2^16` through the real `consume` path is **quadratic** (`2,147,450,880` scan steps, counted exactly), so it exceeded the spike's 120 s socket budget on a slower machine. The frozen capacity was **not** shrunk (forbidden action 6); the limb was given the time that budget costs and passes with the same values. The `O(n)`-per-`consume` eviction was deliberately **not** optimised — that cost is G-3's to measure.* | [g9/REPORT.md](g9/REPORT.md) | [0033](../adr/0033-the-replay-cache-is-a-single-writer-arbiter-process.md), [0027](../adr/0027-the-freshness-window-and-the-replay-cache-budget.md) |
| G-10 | all prior DAG gates | **PASS — THE LAST GATE; THE DAG IS CLOSED.** The benign running example through `B0` and `B3`, **ledger-backed**, producing **all four SS F.1 record types on both arms** (`ObservedRequest`, `MediationEvent`, `ToolIngressEvent`, `EffectEvent` — one each, and `B3` produces an effect because `gt-benign` is the **false-blocking control** it admits) and **all five Part I quantities separately per arm**, every one matching the benign shape: `reference_allow` true, `observed_forwarded` true, `admission_breach` false, `realized_harm` false, `false_block` false. A `false_block` on `B3` would have meant the benign control was being refused; it does not appear. **The source restriction was verified BEHAVIOURALLY on this run rather than inherited from G-12.** Every oracle input was wrapped in a recorder; scoring read **11 distinct field names, none SUT-supplied** — and the `MediationEvent` **carries `reason_code` while the oracle never looked at it**, which is precisely what a source scan cannot show. G-12's L2 proves the source text names no verdict field; this proves the run did not read one. Non-vacuity asserted: the recorder confirms `admitted`, `R`, `C_sets` and `effect_id` **were** read. **The third conjunct — *every prior DAG gate passed* — is CHECKED, not recalled:** the spike runs all fourteen (G-1, G-2, G-3, G-4, G-5, G-6, G-7, G-8, G-9, G-11, G-12, G-13, G-14, G-15) and all pass on this machine. Two are stated rather than counted: **G-3** is adjudicated on the row 9 platform only and **this is that machine**; **G-9's `IA-9`** is verified for the **arbiter** while the ladder's `B3+` keeps the in-process cache by **ADR 0034** — a **decision**, not unfinished business. **Two failing worlds caught:** a suppressed `EffectEvent` makes the record-completeness predicate report failure rather than completing on four-fifths of the evidence; and an oracle reading `self_verdict` is caught by **this gate's own** predicate, not only G-12's scan. **Platform-bound:** `EffectEvent` needs the Win32 exclusive-share ledger, which **does not degrade** (ADR 0014), so off-platform limbs print **NOT ADJUDICATED**, never a green tick. **Scope, in the row's own words: READINESS TO AUTHOR THE CONFIRMATORY CORPUS.** It does **not** mean that corpus exists or that its content is decided (`fixtures/confirmatory/` is empty); it does **not** mean anything is sealed (`PRE_REGISTRATION.md` is a stub); and it establishes **no result about any mechanism** — G-10 certifies the **apparatus**. Deliberately **not** widened beyond its row: the thirteen-scenario, nine-arm, two-configuration machinery exists and passes elsewhere, and running it here would blur what was certified | [g10/REPORT.md](g10/REPORT.md) | [0014](../adr/0014-ledger-platform-decision.md), [0034](../adr/0034-the-campaign-is-single-process-and-b3-plus-keeps-the-in-process-cache.md), [0036](../adr/0036-reference-allow-needs-a-credential-family-gate.md) |
| G-11 | G-6/G-7 | **PASS** — the HTC/INV verifier built at `src/harness/verifier/` implements every §F.2 MUST as a separately named check with its own reason code, so a rejection is attributable. **All fourteen mutations rejected for the condition each targets:** the eight HTC families (wrong-signer, parent-swap, child-swap, depth-rollback, capability-swap, terminal-key-mismatch, domain-tag confusion, expired/`nbf`-violating cert) and the six commitment-layer ones re-tested through the verifier — the latter already `[VERIFIED]` at the commitment layer by the ADR 0003 suite, so this is a **re-test, not a first verification**. Both positive arms pass, **including the `n = 0` zero-hop case**, with no branch keyed on the chain length (behavioural + AST assertions). Two findings recorded rather than smoothed over: the identity-plane check was **masking `htc_chain_linkage`**, so a case was added to isolate it; and the row's blanket "each rejected" **cannot** apply to a *semantically equivalent* container re-encoding, which ADR 0003 requires to be **accepted** with the commitment unchanged — rejecting it would reintroduce the false-rejection bug ADR 0003 fixed (the Part G row was **not** edited). Also closes **`access_token_hash`** (ADR 0018 — distinct from `ath`, `H_JCS` and a bare digest) and **freezes the identity-plane registry** (ADR 0019, `H(R) = d1bfc5ff…`, `frozen_parameters` row 11), which together close G-4's residual limbs **L4** and **L3**. **Residuals:** `label_assertions_digest`/`authz_context_hash` stay category (c) for G-15; the SUT-side INV signer is owed (D21, G-13); the jti cache is G-9; row 5 stays UNSET so F2 `wrong_principal` stays unscored; **IA-3 stays [UNVERIFIED-IA] for G-3** — correctness, not cost | [g11/REPORT.md](g11/REPORT.md) | [0018](../adr/0018-access-token-hash.md), [0019](../adr/0019-identity-plane-registry.md), [0003](../adr/0003-capability-commitment-scheme.md) |
| G-12 | G-2/G-4/G-11 | **PASS** — the SUT now runs in a **spawned child process** (both agents, the arm and the arm's decision); the runner, the mediation record, the ledger writer, the effector, the oracle and every sealed object stay in the parent, and BOTH modes drive **one** stack (`install_boundary`/`install_ingress_recorder`/`build_server` appear exactly once), with in-process still the default so the ten earlier gates are not silently re-adjudicated. **The lying SUT in BOTH directions:** claims BLOCKED while the tool executes (trusted record says admitted, ledger holds the effect) and claims ADMITTED while nothing runs (record says blocked, zero effects, read as *unreached* and not as success) — a gate testing only the first would have tested half. **The oracle is STRUCTURALLY incapable of reading a SUT verdict or digest:** an AST scan finds no `src.sut` import and none of `self_verdict`/`audit_log`/`audit_tail`/`reason_code`/`sut_verdict`/`arm_verdict`/`claimed` named anywhere, shown non-vacuous both by the trusted fields it *does* name and by a counterfactual predicate that reads `self_verdict` being caught. `log_integrity_failure` fires on the lie and stays silent on an honest block **and** on an honest admission. All four correlation faults flagged, with **real** cross-process concurrency (four scenarios, four separate SUT child processes, no cross-talk). **The DROP finding:** a dropped record and an unreached tool both produce ZERO EFFECTS, which Part I reads as no harm — they are told apart because the evidence has three authors (`MediationEvent` in the harness process, `ToolIngressEvent` at tool entry, `EffectEvent` at the effect) and `admitted ⇒ ingress ⇒ effect` is a chain a drop breaks; *zero effects* is read as *no harm* **only** when the earlier records are absent too. **Residual stated:** an adversary who could delete both ledger rows *and* rewrite the mediation record would look consistent — excluded by G-7 and by process separation, not by this predicate. **`raw_arguments` settled (§J.5 item 20):** still a canonical re-serialization, not wire bytes; `realized_harm_F3` stays meaningful because the SEALED digest is the anchor and RFC 8785 only normalizes differences it declares non-semantic — capturing raw wire bytes is handed forward, not assumed fine. **Scope:** oracle independence only; G-6 and G-7 are relied on, not reopened; **IA-3** stays `[UNVERIFIED-IA]` for G-3 and **IA-9** for G-9 | [g12/REPORT.md](g12/REPORT.md) | — |
| G-13 | G-2/G-4/G-11 | **PASS over ALL FIVE strong baselines; the `B2-exchange-task-DPoP` and `B3⁺` limbs are CLOSED** — the independent verifier at `src/harness/verifier/matched_authority.py` (**unchanged by this pass**) recomputes each arm's per-hop authority from **raw presented evidence**, one membership decision per element of `Ω`, and compares it against the **sealed** `C_i`: **38 per-hop equalities over 20 (scenario, arm) cells all hold**, **18 arm-chains** realize the **identical** `C_0 → C_1`, and all **3 × 5** F1 cells block with the cause attributable per arm (`b2_token_scope` / `b3_containment`; chain-tamper `b2_exchange_refused` via the AS's `widening-rar`) while every strong arm admits the benign call. **Two cells have no object and say so:** on chain-tamper **both** exchange arms were refused with **no `AT_1` issued**, which is stronger than the equality they cannot be asked. **Authority is measured, not acceptance** (L1c): every set is unchanged when recomputed at an instant stale by `Δ` — where the boundary demonstrably refuses — and neither the verifier nor the §A.0.1 authorizer imports `src/sut/freshness`. **Every equality shown able to fail** — eight constructed worlds, each judged by the gate's *own* predicate, including the new **L2.W2**: a `B3⁺` replay-cache **denial** leaves `Allowed(P_i)` untouched and is not reported as a granularity mismatch. **D21 adjudicated** on structure + non-reuse + agreement, with the agreement's scope now stated (§F.2 validity only, not the ADR 0027 acceptance window). *Update, 2026-07-31: this row previously read* **PASS over the arms that exist, `B2-exchange-task-DPoP` and `B3⁺` limbs OPEN** *— five arms receive per-hop `C_i` and three existed, so the two unbuilt limbs were open and their formality was recorded as* **expected, not verified**. *Both arms were then built and both limbs RUN; what was expected is now* **verified**, *by per-hop recomputation from raw evidence rather than by assertion — the nine-arm matrix agreeing on outcomes was a prior, not the check, because outcomes are not authority sets.* **Residuals:** the D21 copy-paste-convergence case, which no test closes (review of a change to the verifier is the guard); re-triggered by any `Ω`/`Γ` amendment, any AS-profile change (ADR 0017), any change to `U_task` or the ladder grants (ADR 0024/0029), or any change to `Δ` (ADR 0027). **`IA-3` untouched, stays `[UNVERIFIED-IA]` for G-3 — matched authority, not cost. `IA-9` untouched, stays `[UNVERIFIED-IA]` for G-9 — `B3⁺` carries a `jti` cache, and building it is not running that gate** | [g13/REPORT.md](g13/REPORT.md) | [0017](../adr/0017-as-profile-phase2.md), [0024](../adr/0024-delegating-client-base-token-carries-the-task-grant.md), [0027](../adr/0027-the-freshness-window-and-the-replay-cache-budget.md), [0029](../adr/0029-ladder-row-determines-the-base-grant.md) |
| G-14 | G-12/G-13 | **PASS** — all **three** claims, each measured separately. **C0 (the precondition):** the authenticated-request-id cache attached to `B2-DPoP` and to `B3` is the **same object** (`is`), one class, one shared state — asserted structurally the way G-15 established for the monitor, with a counterfactual showing two *equivalent* caches fail the identity check while both would still block replay. **C1:** both admit first use and both block the bit-identical replay inside Δ (`b2dpop_replay_duplicate` / `b3_replay_duplicate`) — **neither does better than the other**; with no cache attached the same replay is admitted on both, so the blocks are the shared cache's doing. **C2:** on a FIRST-USE request whose tool/arguments differ from what was signed, `B2-DPoP` **admits** and `B3` **blocks** at `invocation_binding` — the cache cannot help because the id is fresh, and the DPoP proof's claims are `ath/htm/htu/iat/jti`, naming method and URI and nothing about the body. **C3:** a bare bearer arm was **given** the cache and the captured token replayed successfully with the cache left holding **0 entries** — it has no **authenticated** request id to consume, so *impossible* rather than *not built*. **THE ATTRIBUTION:** DPoP and invocation binding block different things, and the difference is measured rather than argued — indistinguishable on captured-proof-replay, separated on first-use body mutation, with the residual credited to **INV**, not to the capability. Agrees with §D everywhere measured. **Scope:** an attribution, not a ranking; both arms' §E.5 bitmasks are unchanged (attaching a cache is configuration); **IA-3** stays `[UNVERIFIED-IA]` for G-3 | [g14/REPORT.md](g14/REPORT.md) | [0027](../adr/0027-the-freshness-window-and-the-replay-cache-budget.md), [0033](../adr/0033-the-replay-cache-is-a-single-writer-arbiter-process.md) |
| G-15 | claim-dependent | **PASS** — the F4/F5 reference monitor is **boundary-owned and shared structurally**: one `ContextApprovalMonitor` class object, asserted by identity and by an AST scan finding exactly one definition in `src/`, over one frozen `H(Λ)`, deriving an **identical** `authz_context_hash` on both planes. All **72** F4/F5 cells measured — 4 fixtures × 9 arms × **both** `monitor_attached` configurations — and the `A†` prediction is **reproduced**: the four OAuth arms admit absent the monitor and block with it, at the family's own conjunct. Four failing worlds all caught by the gate's **own** predicates: a monitor attachable to `B3` alone; a cross-arm claim mixing configurations; a cell recorded without its configuration; and a **forged** artifact (real signature, untrusted key) — refused, while the genuine one is accepted, so acceptance is not a hole. **Two findings, both adjudicated into ADRs rather than smoothed over:** `B-cap`'s F3 OAuth-control cells were `NA` for an arm that measurably blocks (**ADR 0031**, `NA → B`), and its F4/F5 cells carried `A†` for a cell that cannot flip (**ADR 0032**, `A† → A`) — one drafting cause, §E.4 filling `B-cap` in as *a capability arm* rather than from its own §E.5 bits. The gate also caught a defect in this block's own renderer, which daggered cells that do not flip. **The residual IS the finding:** with the shared monitor, F4/F5 measure the **monitor**, not the mechanism — no capability-vs-OAuth advantage may be claimed from them either way. **And beside it:** without a monitor configured, `B3`/`B3⁺` refuse the **benign controls** too, because both conjuncts fail closed — so the capability policy plane is useful **only when a monitor is configured**; without one `B3` is not safer on these families, it admits nothing. Both belong in the **results** chapter, not limitations. **Scope:** soundness of the comparison, not superiority of any arm; **IA-3** stays `[UNVERIFIED-IA]` for G-3 and **IA-9** for G-9; row 5 stays UNSET | [g15/REPORT.md](g15/REPORT.md) | [0030](../adr/0030-label-plumbing-and-the-boundary-owned-reference-monitor.md), [0031](../adr/0031-e4-bcap-f3-oauth-negative-controls-are-b-not-na.md), [0032](../adr/0032-e4-bcap-f4-f5-cells-are-a-not-a-dagger.md) |
