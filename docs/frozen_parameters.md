# Frozen (Seal-Time) Parameters — 10 of 11 set, 1 deferred by decision, 0 open

Every parameter below **must** be frozen and hashed before sealing
(`EXPERIMENT_ARCHITECTURE_FINAL.md`, Part H step 3). **Rows 1 (ADR 0026), 2 and 7 (ADR 0025), 3 (ADR 0027), 4, 6 and 10 (ADR 0022), 8 (ADR 0016)
and 11 (ADR 0019) are set. Row 5 is **deferred by decision** (ADR 0028) and is never to be
filled. **Row 9 was the last to be set** (2026-08-02) and is the only one **read** off the
measurement box rather than chosen, so **no row is open**.

*(Dated correction, 2026-08-07, ADR 0044: this sentence read "Row 9 alone is still open", which
contradicted row 9's own entry — SET, 2026-08-02 — three lines below it. Row 11's closing clause
likewise called row 5 "still UNSET" where row 5 reads DEFERRED BY DECISION; a deferred row is not
an unset one, and ADR 0028 turns on exactly that distinction. Both are corrected here; no value
moved, and `src/harness/frozen_parameters.py` reads the same `name = value` tokens before and
after.)* The equivalence
margin and the G-3 latency smoke threshold **must** be fixed from external engineering need
**before any timing measurement** (Part H step 2; Part G, G-3; Part J.2 item 9) — the G-3
threshold first, the equivalence margin separately and after it, both before any confirmatory
timing result. Fixing each value is a decision: record an ADR (`adr/`) and fill in the value and
justification here.

**Machine-readable tokens.** Each scalar row below carries its value as an explicit
`name = value` token, the same idiom rows 4/6/8/10/11 use for `H(Λ)`/`H(Γ)`/`H(R)`.
`src/harness/frozen_parameters.py` is the single reader of every one of them, so no second
copy of a frozen value exists in code, and **any comparison against a row that is not set
fails closed** rather than defaulting.

| # | Parameter | Value | Justification |
|---|---|---|---|
| 1 | Equivalence margin for the "lightweight" claim | **SET (ADR 0026).** `equivalence_margin_ms = 20`. **Estimand:** `median(B3) − median(B0)` over the measured segment, **warm path**. **Decision rule:** the claim stands iff the **upper bound of the 95% bootstrap confidence interval** on that difference is `< 20 ms`; a point estimate below the margin with a CI upper bound above it does **not** support it. **Sampling:** ≥ 200 end-to-end repetitions per configuration across ≥ 3 independent batches, cold and warm reported separately, condition order randomised or Latin-square counterbalanced within each batch, warm-up discarded. **Measured segment:** `presentation + boundary_verification`, each bracketing exactly one call — `arm.present(...)` and `arm.decide(...)` respectively — with instrument bookkeeping, the `MediationEvent`, **every effect-ledger append**, the tool's own execution, `provision` and `delegate` all excluded by name (ADR 0026 §"The measured segment"). **One cell is excluded from every per-arm mean, by name:** on `gt-f1-chain-tamper` the **exchange arms perform a failed AS round trip and receive no token**, while the capability arms do purely local work. Pooling that cell with benign cells would average a network refusal together with local cryptography. **Refusal-path latency is reported as its own series** and is never folded into a benign per-arm mean nor into this row's estimand. This carries into the G-3/RQ4 analysis plan (§J.3 item 12). | ADR 0026: 20 ms is 1% of `T_full` (row 7) and is precisely the per-step operating point published runtime agent-guard work already treats as deployable, so "lightweight" acquires an operational definition rather than remaining an adjective. Deliberately **not** equal to row 2: a gate threshold wants headroom, an equivalence margin encodes what the field tolerates, and they answer different questions. The segment is pinned here because the pre-existing seam bracketed `decide` alone and omitted `B3`'s principal per-invocation cost — an omission that ran **toward** this work's own hypothesis and is fixed before any measurement, not after. |
| 2 | G-3 latency smoke threshold (separate from, and set before, the equivalence margin) | **SET (ADR 0025).** `g3_threshold_ms = 5`. G-3 passes iff the **median single boundary-verification cost** is `≤ 5 ms` **on the row 9 sealed measurement platform**. A Linux CI run of the spike is **regression protection only** and is never adjudicative. | ADR 0025, on three converging arguments: 5 ms is 2% of `T_ttft` and 0.25% of `T_full` (row 7); it is four times tighter than the ~20 ms per-step operating point published runtime agent-guard work treats as deployable, which is defensible for a *smoke* criterion; and Ed25519 plus RFC 8785 canonicalization over the golden-thread payloads sit well below it, leaving roughly three- to tenfold headroom — so **both outcomes are informative** and the gate can fail. Fixed **before** row 1 and independently of it, per Part H step 2. |
| 3 | Freshness window `Δ` | **SET (ADR 0027).** `delta_seconds = 60`, and the **same** `Δ` governs all three consumers: the `jti` replay cache TTL (entries live exactly `Δ`; eviction by age, never by an independent timer), the DPoP proof `iat` acceptance window (fresh iff `|now − iat| ≤ Δ`), and INV freshness at the boundary (identical rule, identical `Δ`). Two conditions travel with it: every consumer takes `now` as an **injected** parameter and none reads a wall clock, and over-window fixtures **advance the injected instant** — real waiting is forbidden and a test asserts no replay path sleeps. Replay-cache budget: `replay_cache_capacity = 65536` (2^16), **fail-closed on overflow** — an insertion that cannot be recorded is a **denial**, never an admission and never a silent eviction of an unexpired entry. | ADR 0027: RFC 7519 §4.1.4/§4.1.5 allow *some small leeway, usually no more than a few minutes* for clock skew and 60 s sits at the conservative end; RFC 9449 leaves the DPoP window to the server, so no specification fixes it for us. `Δ` is an **apparatus constant chosen for comparability**, not a security recommendation. One window because **G-14** attaches the same cache to `B2-DPoP` and `B3` — differing windows would make any difference it observed an artifact of the apparatus in the very gate whose purpose is attribution. 2^16 is three orders of magnitude above the working set, so the budget exists to make the overflow path **reachable and testable**, not to be approached; fail-closed rather than LRU because evicting an unexpired entry would let an attacker replay by first flooding the cache. Availability is deliberately sacrificed to integrity, as apparatus, not advice. |
| 4 | Context-label → {permit, escalate, block} policy | **SET (ADR 0022).** Vocabulary `public ⊏ internal ⊏ sensitive`, derived values take the join; an action is **egress** iff its effect carries a `recipient` (derived from `Ω`, never enumerated — exactly `mail.send` today). Non-egress → permit at every label; egress: `public` → permit, `internal` → escalate, `sensitive` → block absent a valid `DeclassificationArtifact`; an **unlabelled** payload on an egress action **fails closed**. Artifact: `src/harness/policy/label_approval_v1.json`. `H(Λ) = ce4e1e75c782e7bf83cdb7407ace64a91f86683c23ee58c6d9846728814183a7` **(ADR 0023; was `2affd907…` under ADR 0022)** | ADR 0022, composition **amended by ADR 0023**: the smallest three-valued vocabulary in which §A.6's permit/escalate/block ladder is expressible, with `escalate` the only outcome that makes the declassification path observable, and unlabelled-fails-closed because defaulting to `public` would make omitting a label the cheapest bypass of the whole F4 family. Amendable by a later ADR until Part H step 3; any amendment re-triggers the conjuncts that consume it. |
| 5 | `task_authorization_policy` (task → authorized actor principals) for F2 `wrong_principal` | **DEFERRED BY DECISION (ADR 0028) — not an open row awaiting a value, and never to be filled.** The `F2 wrong_principal` subfamily is **deferred and unscored**; the other three `F2` subfamilies (`invalid_credential`, `wrong_holder_proof`/`wrong_dpop_key`, `unauthenticated_caller`) are **retained and scored in full**. A comparison against this row **fails closed** with the deferral named, distinctly from a row that is merely unset. | ADR 0028: every other frozen row anchors to something outside the author's judgement — `Ω`/`Γ` to the golden thread's expressive needs, the registry to §F.2.1, rows 4/6/10 to §A.6's lattice, row 1/2/7 to published figures, `Δ` to clock-skew convention. **Row 5 has no such anchor**: no standard, RFC or reference deployment fixes which principal may carry which task, so any value would be an artifact this author invented and `wrong_principal` would measure conformance to it rather than a property of MCP, A2A or OAuth 2.1 — and it would be indistinguishable in the results tables from the anchored rows. Verified before drafting: `task_authorization_policy` has **no code consumer anywhere in the repository**, so the deferral disturbs no built arm. G-4's `may_act` residual is released by dated note; §E.4's row reads `deferred — unscored`, **not** `NA`, because `NA` would assert the arms cannot express the case, which is false. |
| 6 | Allowed-sink policy for F4 | **SET (ADR 0022).** Two sink classes over the recipient: `internal-sink` iff the recipient's domain is exactly `aasc.local`, `external-sink` otherwise (byte-exact lowercase ASCII, no normalization, matching `Ω`'s encoding rule). Allowed pairs: `(public, internal-sink)`, `(public, external-sink)`, `(internal, internal-sink)`; every other pair only under a valid `DeclassificationArtifact`. Same artifact and `H(Λ)` as row 4. | ADR 0022, composition **amended by ADR 0023**: rows 4 and 6 do **not** compose — row 6 is the **permit whitelist** and row 4 supplies the **severity** of a pair that is not whitelisted, one verdict per cell. `(public, *)` and `(internal, internal-sink)` permit; `(internal, external-sink)` escalates; `(sensitive, *)` and unlabelled block. ADR 0022's "more restrictive wins" reading falsified two of the artifact's own necessity statements and was corrected. Rows 4 and 6 stay frozen in one document because neither decides an egress alone. |
| 7 | Reference LLM-turn denominators (full-turn primary + conservative TTFT) — secondary framing only | **SET (ADR 0025).** `T_full_ms = 2000` (primary) and `T_ttft_ms = 250` (conservative secondary). Both are **fixed constants for interpretation**, never measurements, and neither is ever re-fitted to observed data. **Sourcing obligation (not optional):** each figure is recorded with an **exact source, URL and retrieval date**, snapshotted at seal time; the primary anchor is the independent benchmark publishing per-model TTFT and total response time on a fixed methodology, and trade-press aggregations may corroborate but **must not** be the sole citation. If a figure cannot be sourced to a dated, retrievable publication the value stays and the claim resting on it is reported as **unanchored** — the value is not changed to whatever happens to be citable. **Sources, recorded (ADR 0042; snapshot at seal time still owed):** primary anchor for both denominators is Artificial Analysis, the independent benchmark publishing per-model TTFT and total response time on a fixed, documented methodology — https://artificialanalysis.ai/methodology/performance-benchmarking (retrieved 2026-08-06) and https://artificialanalysis.ai/models (retrieved 2026-08-06). | ADR 0025: a denominator that is too **large** would make any overhead look negligible, so both values are taken at or below the **low end** of published 2026 ranges and the reported fraction is therefore **larger** than a generous choice would give — the "lightweight" framing is held to a stricter standard rather than flattered. `T_full = 2000 ms` sits at the bottom of the one-to-five-seconds-per-reasoning-step range; `T_ttft = 250 ms` is at or below the mainstream fastest tier (specialised silicon near 0.18 s, mainstream low-latency near 0.35 s, frontier non-reasoning 0.4–0.8 s). Row 7 governs a **secondary interpretive aid only** — no hypothesis, gate criterion or retraction rule depends on it except through row 2 — so its being fixed and disclosed **in advance** matters far more than its precision. |
| 8 | `Ω` (action/resource ontology) and `Γ` (authorizer configuration) — frozen and hashed as `H(Γ)` | **SET (ADR 0016).** `Ω` = 7 `(action, resource)` elements over 5 tools; `Γ` = the MSc-profile authorizer plus its matched `−attenuation` ablation. Artifact: `src/harness/authorizer/omega_gamma_v1.json`. `H(Γ) = f63320c9da3731a6ea04dc51d9f6852f3a3e130182ce3a7fe251158751333deb` | ADR 0016: the smallest vocabulary in which the golden thread, amplification, two-hop strict attenuation and every retained attack family are expressible, with `Γ` written against the four G-2 criteria; amendable by a later ADR until Part H step 3, and any amendment re-triggers G-2 and the G-4 effective-authority limb. |
| 9 | Sealed measurement platform — OS + exact Windows version/build (decision: ADR 0014; the campaign runs on Windows, the ledger enforcement is Win32-only) | **SET (2026-08-02, read from the machine).** `sealed_platform = Windows 25H2 build 26200.8875; 12th Gen Intel(R) Core(TM) i7-12700H; 14C/20T (12P/8E logical); 15.75 GiB DDR4; power scheme da75b896-eea0-461c-a43a-73a73caf9f43 (High performance), on AC` | The only frozen row that is **read rather than chosen**. Every field is machine-read by `src/harness/measurement_platform.py`; the Commander's transcription was a cross-check, and the machine-read value is what is sealed. Full record, including the P/E-core split detected through `GetSystemCpuSetInformation` and the hazard state as found, in `docs/measurement_platform.md`. ADR 0025: **any change to this row invalidates a G-3 adjudication on the previous platform** — so a power-plan, hardware, driver or Windows-build change re-triggers G-3. |
| 10 | Oracle classification policy — the **high-risk action set** (Part I `is_high_risk`, F5/approval) and the **sensitive-label set** (Part I `is_sensitive`, F4/egress) | **SET (ADR 0022).** `high_risk_actions = {mail.send, notes.delete}`; `sensitive_labels = {sensitive}`. Same artifact and `H(Λ)` as rows 4 and 6. | ADR 0022: `mail.send` is externally visible and `notes.delete` irreversible, whereas `calendar.read`/`notes.read` are reads and `notes.write` is reversible inside the user's own store. Registered (not decided) by gate G-2, which exposed that neither set is owned by any other row — and fixed here **in the same ADR as row 4**, exactly as this row required, because `sensitive_labels` depends on row 4's vocabulary. |
| 11 | Identity-plane registry (§F.2.1) — `actor_of(·)` → exactly one principal → exactly one `htc_holder` key, plus the recorded resource owners — frozen and hashed as `H(R)` | **SET (ADR 0019).** 3 principals + the AS root key, 3 actor claims, 1 resource owner. Artifact: `src/harness/verifier/identity_registry_v1.json`. `H(R) = d1bfc5ffcb22e2ded736f5248b99b9f019ba314b93ddd808e50ea522b3fb4cbe` | ADR 0019: every entry carries a stated necessity and the loader rejects one without it; the artifact fixes **structure and derivation labels, not key bytes**, because holder keys are per-campaign material from sealed seeds (ADR 0007) — the same line ADR 0016 drew for `Γ` and `κ`. Amendable by a later ADR until Part H step 3; any amendment re-triggers G-11 and G-4's `actor→holder` limb. **Not** the `task_authorization_policy` (row 5, DEFERRED BY DECISION — ADR 0028 — which is not the same as unset, and the distinction is the whole of that decision). |

**No number in this document was measured.** Rows 1, 2 and 7 fix the bars and the denominators **before** any timing measurement, exactly as Part H step 2 and §J.2 item 9 require; a threshold chosen after seeing the data is a description of the data wearing a threshold's clothes. `IA-3` stays `[UNVERIFIED-IA]` until **G-3 actually runs on the row 9 platform** — fixing a threshold is not verifying an assumption, and none of ADR 0025/0026 may ever be cited as if it were.

Related frozen artifacts sealed alongside these values (Part F.2.1, Part H step 3): the hashes of all
configuration in the sealed candidate manifest (`seal/manifest_v0.6.json` at the time of writing; superseded manifests are kept under `seal/superseded/` and never deleted). *(The identity-plane registry was named here as a
related artifact and is now row 11, set by ADR 0019.)*
